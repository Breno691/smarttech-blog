// Cena Three.js do painel/dashboard flutuante da seção "Sistema", com GSAP
// ScrollTrigger girando/aproximando o painel conforme o usuário rola a página.
//
// Só é carregado via import() dinâmico, depois que device-capability.ts confirmar
// que o aparelho aguenta — nunca baixado em modo 'reduced'.
//
// Implementado na FASE 4.

export function initDashboard3D(_mount: HTMLElement): void {
  // TODO (Fase 4): cena Three.js (painel estilizado, MeshStandardMaterial, luzes)
  // + gsap.ScrollTrigger com scrub ligado ao progresso de rolagem da seção
}
